#!/bin/bash
# ----------------------------------------------------------------------------

set -euo pipefail

# ----------------------------------------------------------------------------

DB=osm

# ----------------------------------------------------------------------------

osm2pgsql-replication update -d "$DB"

osm2pgsql-gen -d "$DB" -j16 -a

# ----------------------------------------------------------------------------
